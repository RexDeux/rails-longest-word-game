<div class="container">
    <h1>Rake Em' Up, The Game Begins Anew</h1>
    <ul class="letters">
        <% @letters.each do |letter| %>
            <LI><%= letter %></li>
        <% end %>
    </ul>

    <form action="/score" method="POST">
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
    <label for ="">What is the longest word you cna find?</label>
    <div class="input">
        <input type="text" name="word">
        <input type="submit" value="Play">
    </div>
   </form>
</div>